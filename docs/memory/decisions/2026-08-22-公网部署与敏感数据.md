# 决策记录：公网部署与敏感数据（2026-08-22）

> 依据：用户直接指令（2026-08-22），已写入 design.md §10 决策 3/6/7

## 决策

| # | 决策 | 内容 |
|---|------|------|
| 1 | autocommit 变更 | 默认**开启**，每日 0 点自动提交一次；用户可主动触发（`git-commit` 子命令） |
| 2 | 公网部署 | 8931 端口对公网开放（云安全组已放行），**IP 白名单**访问控制 |
| 3 | 首次数据导入 | 密码薄.xlsx（无图片，57 行）→ CSV 存 `structured/password-vault.csv`；服务器地址/root 密码/数据库密码 → `structured/servers.csv` |
| 4 | 记忆库剥离项目仓库 | 记忆库整体 gitignore 不入代码仓库；骨架模板移至 `docs/memory-template/` |
| 5 | **记忆异地备份（2026-08-22 补充）** | **记忆库（含敏感数据）推送到 Gitee 独立私密库**（用户知情并决定，如 `ZipperAgentMemory-memory`）；与代码仓库分离 |

## 安全边界（已执行）

- ✅ `memory/` 已从项目仓库剥离（`git rm --cached` + `.gitignore` 加 `memory/`）
- ✅ 密码薄 CSV / 服务器密码 CSV 仅存本地 `memory/`，`git check-ignore` 确认不入库
- ✅ 敏感数据只在服务器本地记忆库（独立 git 仓库），经迁移脚本跨机复制
- ⚠️ 待办：IP 白名单需用户提供允许访问 8931 的 IP 列表

## 数据位置

- 本地记忆库：`D:\Works\ZipperAgentMemory\memory\`（含 sensitive CSV，不入项目仓库）
- 部署目标：服务器 `/opt/zipper-agent-memory/memory/`
- Gitee 项目仓库：仅代码 + 文档 + 记忆库骨架模板，无任何敏感数据
