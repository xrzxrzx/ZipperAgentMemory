# 记忆库统一事件记录（2026-08-29）

> 事件：用户报告「其他会话写入记忆库但服务器未收到」→ 排查定位 → 修复统一

## 真相

DSH 的 MCP 配置（`~/.dsh/dsh-mcp-client-v2.json`）此前指向**本地 stdio 记忆库**
（`D:\Works\ZipperAgentMemory\memory`），而服务器记忆库（`/opt/zipper-agent-memory/memory`）
是另一份。用户在 D:\学习 等 DSH 会话写入的记忆进了**本地库**，未到服务器 → 每日
git 同步（服务器→Gitee）当然没有这些内容。

**关键证据**：
- DSH MCP 配置：stdio + `D:\Works\ZipperAgentMemory\memory`
- 本地库有 `notes/学习规划.md`（专升本备考核心记忆）、`dsh-env/plugins.md`、`dsh-ui-fix-2026-08-26.md`
- 服务器库此前只有 11 个文件（部署骨架 + 密码薄/服务器密码）

## 修复动作

1. **DSH MCP 配置改为 HTTP**：`~/.dsh/dsh-mcp-client-v2.json` → `http://8.141.89.50:8931/mcp`（需重启 DSH 生效）
2. **本地库独有内容合并到服务器**：
   - `notes/学习规划.md`（7593B，专升本备考全记录）
   - `dsh-env/plugins.md`（DSH 插件排障）
   - `dsh-ui-fix-2026-08-26.md`
3. 重建索引（13 files）、主动提交、推送 Gitee（提交 `15e602f`）

## 教训（沉淀）

1. **单一真源原则**：多客户端共享记忆必须统一指向同一记忆库（服务器），禁止本地/远程双库并存
2. **客户端配置审计**：DSH 的 MCP 配置是独立文件（`~/.dsh/dsh-mcp-client-v2.json`），
   与 Claude（`~/.claude.json`）、Codex（`~/.codex/config.toml`）各自独立，改一处不生效于他处
3. **待办**：DSH 重启后验证 MCP 连接指向服务器；本地库 `D:\Works\ZipperAgentMemory\memory` 保留为开发副本（gitignore 不入库），如需彻底废弃可删除
