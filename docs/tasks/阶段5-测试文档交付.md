# 任务书（草稿）：阶段 5 —— 测试、文档、交付

> 状态：**草稿，待设计文档批准后生效**。

## 任务书：阶段 5 测试、文档与交付

- **派发者**：协调者
- **依据**：`docs/design.md` §8 阶段 5；`AGENTS.d/testing.md`、`AGENTS.d/docs.md`、`AGENTS.d/review.md`
- **目标**：全量测试、文档同步、部署交付，完成项目验收
- **范围**：
  1. **测试补全**：pkg/memory、pkg/index、pkg/watch、pkg/mcp、pkg/git 单元测试补全（表驱动优先）；集成测试（MCP 握手端到端、迁移脚本端到端、watcher→索引→搜索链路）
  2. **代码审查**：按 AGENTS.d/review.md 双轴审查（规范轴 + 规格轴），阻塞项修复
  3. **文档同步**：README 使用文档（安装/配置/迁移/客户端接入）；docs/memory 项目记忆最终化；design.md 与实现一致性核对
  4. **部署交付**：按 `docs/memory/server-评估.md` 部署规划执行——服务器装 Go ≥1.25 + git；clone 项目；`go build`；systemd service 托管 `zipper-agent-memoryd serve`；记忆库初始化
  5. **资源实测报告**：常驻内存（<60MB 验证）、空闲 CPU、启动耗时，写入交付报告
- **验收标准**：
  1. `go test ./...` 全绿、`go vet ./...` 零告警；
  2. 审查报告阻塞项全部清零；
  3. 文档与实现一致（抽查关键章节）；
  4. 服务器部署跑通：systemd 启动 → MCP 客户端连接 → 读写搜索可用；
  5. 资源实测报告达标（内存红线）。
- **交付物**：测试报告、审查报告、README、部署记录、资源实测报告（`docs/验收/阶段5.md`）
- **约束**：涉及代码修改先走审查；部署在用户确认后进行（服务器操作先确认再执行）
- **DoD**：5 项验收通过；向用户做最终交付汇报
