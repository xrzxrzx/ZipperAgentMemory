# 部署验收记录（2026-08-22）

> 服务器：8.141.89.50（阿里云 Linux 3，2 核 / 1.8GB）　服务：zipper-agent-memory（systemd）
> 目标路径：/opt/zipper-agent-memory　监听：0.0.0.0:8931（公网，IP 白名单）

## 1. 部署环境

| 项 | 状态 |
|----|------|
| git | ✅ dnf 安装 2.43.7 |
| Go | ✅ 官方 tarball 1.26.7（/usr/local/go）；GOPROXY=goproxy.cn（proxy.golang.org 服务器不可达，已切换） |
| 代码 | ✅ scp 传输（Gitee SSH 未配服务器密钥，HTTPS 私库需认证→本地打包上传） |
| 二进制 | ✅ go build 18MB（Linux amd64） |

## 2. 记忆库部署

- memory/ 完整导入（11 文件，含 password-vault.csv、servers.csv 敏感数据）
- `git-init` 成功（本地身份 zipper-agent-memory）
- `rebuild-index`：10 files indexed

## 3. 搜索验证（FTS5）

| 查询 | 结果 |
|------|------|
| P站（中文） | ✅ 1 hit（密码薄） |
| QQ | ✅ 1 hit |
| xrz（密码关键词） | ✅ 命中多条 |

## 4. MCP 服务验证（公网直连 120.228.126.4）

- initialize 握手：HTTP 200 ✅（返回 capabilities + 中文 instructions）
- tools/list：6 工具带行为标注 ✅
- tools/call memory_search「P站」→ 命中 ✅

## 5. 服务运行指标

| 项 | 实测 |
|----|------|
| systemd 状态 | active (running)，enabled 自启 |
| 常驻内存 | **11.3M**（systemd CGroup 实测，红线 60MB 的 1/5） |
| 端口 | 0.0.0.0:8931 监听 |
| IP 白名单 | `MCP IP allowlist enabled: 2 entry(ies)`（127.0.0.1 + 120.228.126.4） |
| autocommit | `daily commit at 00:00 local`（每日 0 点） |
| 配置加载 | `loaded config from /opt/zipper-agent-memory/config.json` |

## 6. 部署中的环境问题与解决

| 问题 | 解决 |
|------|------|
| proxy.golang.org 服务器不可达 | GOPROXY 切 goproxy.cn（go env -w + /etc/profile.d） |
| Gitee clone 需认证（服务器无 SSH key） | 本地 tar 打包 scp 上传（含 .git） |
| heredoc 写 config.json 引号丢失 | scp 上传正确 JSON |
| Windows tar SCHILY.fflags 警告 | 无害，忽略 |

## 7. 遗留事项

- [ ] 服务器 Gitee SSH key（如需服务器直接 git pull/push 更新代码，需生成密钥加 Gitee 部署密钥）
- [ ] 家庭 IP 变化时更新 config.json 的 allow_ips 并 `systemctl restart zipper-agent-memory`
- [ ] 在外笔记本使用 SSH 隧道：`ssh -N -L 8931:127.0.0.1:8931 minichat-server`，客户端指向 http://127.0.0.1:8931/mcp

## 8. 结论

✅ 部署成功，功能完整：记忆库读写/搜索/MCP 公网访问（白名单保护）/每日定时提交/主动提交全部可用。
资源红线满足：内存 11.3M（<60MB），空闲 CPU≈0（事件驱动）。
